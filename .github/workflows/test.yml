---
name: JS Actions

on:
  pull_request:
    types: [labeled]

jobs:
  action:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      # Testing using the current dir
      - name: hello-action
        uses: ./action.yml
